[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pgjdbc-client"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = ["JayDeBeApi>=1.2.3", "JPype1>=1.4.1"]

[tool.setuptools.packages.find]
where = ["src"]
